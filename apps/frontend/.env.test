# Swaps the Auth0 login redirect, which cannot run unattended, for a provider
# that fetches a real signed token from the local issuer. The backend still
# verifies every request.
VITE_TEST_MODE=true
VITE_TEST_ISSUER_URL=http://localhost:9999
VITE_TEST_SUBJECT=auth0|e2e
