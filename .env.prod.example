# Database (external PostgreSQL on VM)
DATABASE_URL=postgresql://username:password@postgres-host:5432/flask_react_db
# TEXT_EXTRACTOR_URL=http://localhost:6004                    # Local dev
TEXT_EXTRACTOR_URL=http://text-extractor:6004            # Docker Compose (use service name)
# Auth0
AUTH0_DOMAIN=your-tenant.auth0.com
AUTH0_CLIENT_ID=your-client-id
AUTH0_CLIENT_SECRET=your-client-secret
AUTH0_AUDIENCE=your-api-audience

# App
SECRET_KEY=change-this-to-random-secret-key
FLASK_ENV=production

# Storage - Option 1: Google Cloud Storage (recommended for production)
# GCS_BUCKET_NAME=your-bucket-name
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Storage - Option 2: Local file storage (default if GCS_BUCKET_NAME not set)
# Files stored in ./uploads volume mapping

# Frontend API URL (publicly accessible URL)
VITE_API_URL=https://your-subdomain.example.com/api
